/**
 * Frameworks & Drivers — plugin de persistência.
 *
 * Entidades JPA, repositório Spring Data, implementação da porta AccountRepository e
 * mapeamento domínio<->entidade. Depende apenas pra dentro: a porta vive na application
 * e o agregado no domain.
 */
package br.com.digital.wallet.infrastructure.persistence;
