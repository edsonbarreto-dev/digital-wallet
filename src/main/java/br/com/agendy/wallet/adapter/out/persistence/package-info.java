/**
 * Adaptadores de SAÍDA — persistência (JPA).
 *
 * Implementam as portas de saída da aplicação usando banco de dados: entidades JPA,
 * repositório Spring Data e mapeamento domínio<->entidade. Detalhes de framework ficam aqui.
 */
package br.com.agendy.wallet.adapter.out.persistence;
